# 从窗口建立到现在的全流程审计

## 2026-08-08｜背景虚化与景深规则审计

### 复查结论

- 《口播提示词总规则》原来只要求“需要时补充景深、焦点转移和运动模糊”，没有强制每条成片必须出现背景虚化，也没有要求填写光圈、镜头、器材、对焦主体和人物/背景距离。
- 母库原来有 `Rack Focus`、`运动模糊` 和 `Directional motion blur`，但这些不等于背景虚化；并且曾出现 `deep depth of field`，与用户要求的背景虚化方向相反。
- 三份文档原来没有明确后期虚化边界，也没有`DOF_FAIL`或`DOF_CONSTRAINT_CONFLICT`门禁。

### 已处理

1. 背景虚化已提升为默认硬门禁：人物眼睛、嘴部和面部表演清晰，背景必须明显失焦并形成自然散景。
2. 每条Omni提示词新增摄影器材/镜头、焦距或等效焦距、宽光圈或等效景深、对焦主体、人物与背景距离、前中后景层次和连续对焦字段。
3. 被拍/固定机位/第三视角默认使用全画幅电影机或大底无反、50mm/85mm等效人像镜头、约f/1.4–f/2.8宽光圈；手机自拍使用人像模式或大底主摄的自然景深过渡。
4. 明确运动模糊不能替代景深；后期只能在真实光学散景基础上做轻微人物/背景跟踪增强，禁止整屏高斯模糊、静态蒙版和粗糙抠图。
5. 背景过近、平面墙面或空间没有层次时，先调整人物离墙、机位、景别或背景距离；无法调整时标记`DOF_CONSTRAINT_CONFLICT`。
6. 生成结果背景仍清晰、脸部失焦、焦点漂移、头发边缘光晕或背景闪烁时标记`DOF_FAIL`，不得放行。
7. 已将规则同步到网页版 Skill、启动提示词、ZIP、公开HTML和GitHub Pages，需在外部生成时实际回收视频证据后再做DOF QC。

### 当前仍需真实结果验证

- 提示词可以强制写入实现条件，但不能在未生成视频前证明虚化已经出现；用户返回视频后必须逐条检查人物清晰度、背景失焦、散景自然度、边缘质量和焦点稳定性。
- 若用户提供的场景图本身没有足够空间纵深，必须先处理`DOF_CONSTRAINT_CONFLICT`，不能只在负面提示词里写“背景虚化”。

## 2026-08-08｜本轮输入方式、情绪表演和声音参数修订

### 本轮已经处理

1. **用户输入改为固定填空表**：每条任务由用户同时提交逐字文案、人物图、场景图、服装图和大概动作；还可补充身体状态、状态转换、拍摄关系、人物视线、情绪和镜头倾向。没有图片必须明确写“无”，本窗口不再擅自编造完整人物、服装或场景。
2. **情绪表演改为放大、夸张、可见**：每个时间段必须根据台词写放大的微表情、带心理动机的头部/视线动作和一个身体或手势反应；仍受“最多两个主要身体状态、最多一次状态转换、每段最多一个主要动作变化”约束。
3. **“音色”从当前配音控制参数移除**：固定刘泽 `speaker_id=liuze` 保留；当前只填写语速、语调、情绪、发声方式、节奏、音调变化、停顿、重音和呼吸。已同步总规则、网页版 Skill、启动提示词、项目声音协议、声音队列和 Omni 声音 Skill/schema。
4. **未测音频不得先拆最终Omni时间轴**：没有真实 VoxCPM2 WAV 时只输出配音稿、`ESTIMATED_DURATION` 和 `OMNI_PENDING_AUDIO`；收到WAV后改为 `MEASURED_DURATION`，按自然语义的最少片段数拆分。
5. **文字与标点门禁补强**：台词逐字保留；标点只允许做最小节奏优化；疑似错字、转写异常或语义异常必须先列待确认，不能默默修正；VoxCPM2可复制区只放最终台词。

### 当前仍未完成、不能被提示词伪装成完成的事项

1. **本条具体任务的真实音频尚未随本轮输入提供**：因此现在只能先做输入校验和配音稿，不能给出最终实测时间码或最终片段数量。
2. **人物/场景/服装/动作图片需要用户实际附上**：规则已经固定输入字段，但没有附件时不会自动获得参考图锁定、@me选择证据或动作可行性证据。
3. **提示词“高于对标”仍需生成结果后验证**：规则可以保证比较维度、返工门禁和用户输入映射，不能在未看到候选图/视频时凭文字宣称已经超过对标。
4. **Omni无法仅靠提示词保证帧级精准卡点**：最终必须使用实测音频、片段首尾状态和必要的后期校准；提示词只负责阶段顺序、口型意图和镜头承载。
5. **外部生成仍由用户手动完成**：本窗口只输出可复制提示词和门禁；用户需要返回设置截图、生成结果、尺寸/时长和媒体证据后，才能做生图/视频QC。
6. **历史审计和历史会话中仍可能出现旧的“音色”字段**：这些只作为历史记录，不属于当前执行字段；当前规则、网页版包和项目声音协议已明确以“移除音色后的字段集”为准。

### 本轮涉及的主要文件

- `口播提示词总规则.md`
- `chatgpt-web-koubo-prompt-executor/SKILL.md`
- `chatgpt-web-koubo-prompt-executor/START_PROMPT.txt`
- `02_audio/voice_generation_protocol.md`
- `02_audio/voice_queue.json`
- `01_intake/reference_analysis.md`
- `review_queue.md`
- `/Users/m4air/.codex/skills/omni-voice-direction/SKILL.md`
- `/Users/m4air/.codex/skills/omni-voice-direction/references/voice-profile-schema.json`

## 2026-08-07｜风格色调前置与母库Markdown化

- 已将“风格色调”设为每条Omni提示词第一行，最低硬要求为“现实主义、真实实拍感、自然物理反馈”；不得只写抽象的“电影感”。
- 已将含完整对标提示词附录的DOCX转换为Markdown母库：`口播视频全要素制作规范_含对标提示词附录_2026-08-07.md`。
- Markdown转换保留正文、标题、列表、表格和四段对标提示词原文；原DOCX保留为历史版式备份，不再作为日常同步入口。
- 以后网页版ChatGPT与Codex若接入同一Git仓库，应以Markdown文件为共享源；每次修改必须提交变更记录，避免各自生成无法互相看到的静态HTML。

## 2026-08-07｜网页版ChatGPT执行Skill包

- 已生成网页版ChatGPT可接收的ZIP执行包：`ChatGPT网页版口播提示词执行Skill_2026-08-07.zip`。
- ZIP包含SKILL.md、START_PROMPT.txt、四份Markdown规则参考文件和网页版执行元数据。
- Skill只负责当前两步：VoxCPM2配音文字稿＋Omni视频提示词；不假装生成图片、视频、音频或操作外部网页。
- 网页版使用时应先上传ZIP，再粘贴START_PROMPT.txt；如果附件读取失败，回退到公开规则网页。
- 后续本窗口对外回复固定附上三份本地规则文档，并在最底部附上公开规则网页。

审计范围：本原创从零到一窗口、孩子教育号批量项目、首条视频0001。

审计结论：当前不是“全部流程已经闭环”，而是“输入整理完成、0001人物/场景母版锁定、刘泽配音技术生成完成、分镜和4K门禁未完成”。外部图片/视频生成今后由用户手动在Google Flow等窗口执行；本窗口只负责准备包、步骤门禁、回收证据、比较、QC、记录和后续剪辑编排。

## 2026-08-07｜提示词规则缺口修订结果

本轮将此前列出的14项提示词缺口集中落实到《口播提示词总规则》；该文件是提示词生成的唯一权威规则，本审计文件只记录落实状态，不复制整套规则。

| 编号 | 问题 | 处理位置 | 当前状态 |
|---|---|---|---|
| 1 | 时长与4/6/8/10秒拆分不够硬 | 《口播提示词总规则》“当前唯一硬性要求清单”“时长、配音和拆分” | 已写入规则；具体音频仍需每条实际复测 |
| 2 | VoxCPM2声音格式不完整 | “固定默认声音”“声音与口型原则” | 已补齐固定声线＋变化项格式；默认刘泽不重复冗长档案 |
| 3 | 9:16没有成为硬门禁 | “当前唯一硬性要求清单”“Omni提示词固定字段” | 已写入，默认竖版9:16 |
| 4 | 参考图和@me调用不够明确 | “参考图规则” | 已写入；用户选择，提示词只锁定和写必要变化 |
| 5 | 每段强制堆眼神、表情和动作 | “每个时间段必须具备”“动作密度限制” | 已修正为至少一个可见表演结果，其余允许自然延续 |
| 6 | 摄影机运动、焦点、视差和动作范围不足 | “镜头原则”“景别与动作范围匹配” | 已补入；要求区分实际移动与数字变焦 |
| 7 | 高动态奔跑模式没有落地 | “台词驱动的连续拍摄状态” | 已改为状态转换，不再使用模式C；奔跑按台词条件调用 |
| 8 | 环境声、动作声和音乐规则不足 | “声音与口型原则” | 已补入人物声、环境声、动作声、可选音乐和人声优先级 |
| 9 | 无参考图时的场景与灯光缺少决策逻辑 | “无参考图时的场景与风格决策” | 已补入台词意图→现实条件→场景→灯光顺序 |
| 10 | 缺少台词意图层 | “Omni提示词固定字段”“每个时间段必须具备” | 已补入，每段必须标注台词作用/语言意图 |
| 11 | 无字幕、无乱码、无额外文字未硬性规定 | “当前唯一硬性要求清单”和负面限制 | 已写入 |
| 12 | 缺少复制前质量检查 | “最终交付格式” | 已补入台词、音频、转换、口型、镜头、参考图和负面限制检查 |
| 13 | 多片段首尾连续性不足 | “时长、配音和拆分”“连续拍摄状态” | 已补入姿势、视线、手位、重心、镜头、环境和声音承接 |
| 14 | 历史记录可能与当前规则混淆 | 本审计文件保留历史；《口播提示词总规则》声明唯一有效规则 | 已处理为“规则单一、审计留痕”；历史内容不删除，当前状态以最新条目为准 |

说明：上述“已写入/已补入”只代表规则层已经落地，不等于0001的外部生图、实际音频听感、分镜和视频已经完成；这些仍按本文件后续门禁执行。

### 2026-08-07追加修订：主要身体状态上限

用户进一步明确：一条视频不要串联过多动作。已将规则改为：单条4–10秒视频最多两个主要身体状态，最多一次状态转换。允许坐→站、站→走、走→跑、跑→停中的一个；禁止坐→站→走、站→走→跑等三状态串联。眼神、呼吸、点头、轻微手势仍可存在，但不算新的主要身体状态，且必须服务台词。

### 2026-08-07追加修订：肢体动作来源与优先级

肢体动作不再由通用模板自动决定。生成提示词时先读取用户在聊天窗口提供的场景图，依据人物位置、空间、道具和景别判断动作可行性；若用户随文案明确指定动作，则用户指定动作优先。只有没有场景图、也没有用户动作要求时，才调用通用口播动作库。若指定动作与场景图冲突，必须显式指出并协商调整，不能默默替换。

### 2026-08-07追加修订：人物面部无胡茬

所有Omni人物提示词必须加入面部干净、无胡茬、无胡须、无面部毛发残留的硬性约束。即使人物参考图或`@me`原始形象带有胡茬，也必须作为必要修正写入提示词，不能直接继承。该要求同时放入负面限制和复制前检查。

## 一、已经正确落实的要求

1. 窗口职责已改为从主题/题材/一句话开始，完成故事、脚本、分镜、提示词、声音、视频、剪辑和验收的原创全流程，不再把本窗口定义成单纯复刻窗口。
2. 孩子教育号批量源文件已入库；高考金榜题名题材已跳过，当前保留18条候选。
3. 0001已建立独立项目，使用了正确的0001对标视频帧，不再误用空场景截图。
4. 0001已使用Google Flow Nano Banana Pro实际生成4张初始候选；候选03已被用户确认并锁定为人物/场景母版。
5. 已建立图生图优先规则：对标图实际上传、保留/删除/增强映射、一次x4、先内部对标、通过后才展示。
6. 已建立真实贴合总规则：季节、时间、天气、地点、服装、场景、道具、台词、镜头、声音和剪辑必须处于同一现实条件。
7. 已建立大气与文化点睛规则：真实尺度、视觉主锚点、前中后景纵深，国学元素只做有语境的少量点睛。
8. 已建立快节奏规则：0.0–0.8秒钩子、前3秒至少3个有效变化、镜头默认0.6–1.8秒、轻微手持呼吸感、侧面/三分之四/过肩/POV覆盖。
9. 历史版本声音JSON字段曾覆盖音色、语速、语调、情感、发声方式、节奏、音调变化、呼吸、标点和时间码；当前执行版本已移除“音色”字段，只保留固定刘泽身份和其余控制字段。
10. VoxCPM2任务已实际完成：刘泽、`liuze`、CUDA、48kHz/16-bit/单声道、WAV、时间轴、Hash和技术QC均有证据。
11. VoxCPM2 Worker已增加Claim、Running、Heartbeat、超时、Error和声线精确匹配规则，解决了“领取后无输出无限卡住”的流程缺口。
12. Obsidian摘要、时间线和交接文件已持续写回，能够记录当前状态和跨电脑接手信息。

## 二、已经发现但尚未彻底解决的问题

### 1. 生成数量硬规则曾被执行错误

用户要求所有Nano Banana Pro生图使用`9:16 + x4`，一次生成4张。之前0001分镜重试时错误选择了x1。这是本窗口操作错误，不是Flow问题。

必须修改为：所有图片生成请求在提交前，记录截图或页面证据，确认模型、比例和`x4`；没有x4证据，不算已提交。

### 2. 外部生成执行责任已调整

用户已明确：图片和视频生成由用户手动在其他窗口执行，本窗口不再负责代操作。批量manifest、生成脚本、总控Skill、生产OS、本土化Skill、图片提示词Skill、视频生成Skill及相关参考文件已统一为`USER_MANUAL_EXTERNAL_SURFACE_GOOGLE_FLOW_OMNI`边界。自动浏览器/模型提交只有在用户针对本次运行明确授权时才允许。

新边界应是：本窗口输出可复制提示词、参考图清单、模型设置清单、提交检查表和回收命名规则；用户手动生成后，把结果/截图/媒体ID/尺寸/设置证据交回，本窗口再做比较和QC。

### 3. 分镜没有真正完成

当前0001只有分镜表和提示词包，以及候选05/06的首轮结果；候选05只是视觉方向有效的单张图，候选06有错误字幕和发型漂移，不能作为完整S01–S04分镜。

正确流程应是：S01、S02、S03、S04各自独立提交一次，每次都`x4`，共16张候选，再按镜头对应关系、连续性、无文字、对标超越和分辨率门禁筛选。

### 4. 4K门禁仍未解决

Flow返回的候选实际为768×1376或720×1280，未达到竖版4K `2160×3840`。因此不能把这些图片标为4K，也不能直接当作最终图片资产。

需要在流程中明确三种状态：`NATIVE_4K_PASS`、用户明确批准后的`UPSCALED_4K`、`4K_OUTPUT_UNAVAILABLE`。第三种状态不得进入视频生成。

### 5. 图生图“超过对标”缺少统一可复核评分

现在有内部比较结论，但评分仍偏人工描述，缺少固定的逐项证据表。后续必须固定比较：人物一致性、构图、场景尺度、现实贴合、灯光、材质、前中后景、文化克制度、镜头可用性、分辨率；总分必须高于对标至少10%，并保存并排图和评分表。

### 6. 提示词编辑曾发生追加污染

Flow富文本编辑框使用普通填充时曾把新提示词追加到旧提示词后面，导致一个输入框里重复多版S01指令。这不是提示词创意问题，而是输入操作问题。

手动流程必须要求用户在提交前清空输入框，确认只剩一版提示词；每个S01–S04使用独立、短而完整的提示词，不把多版指令堆在同一个输入框。

### 7. 台词时长与Omni单条上限发生冲突

用户规定Omni单条支持4/6/8/10秒，超过10秒要拆分。0001的第二段刘泽音频实际为11.84秒，因此当前“两个10秒片段”的规划并未真正满足上限。

必须在生成视频前新增“实际配音时长门禁”：如果任一段超过10秒，不能直接送Omni。要么在不改原文的前提下重新调整自然语速并复测，要么按自然语义拆成第三段；不能硬截断、不能删字、不能把11.84秒塞进10秒。

### 8. 刘泽配音只完成技术QC，听感QC仍未完成

当前刘泽音频已经通过文件、格式、Hash、时间轴和无削波检查，但尚未完成用户听感确认。还不能标记`authoritative=true`。

听感QC必须检查：声线是否确为刘泽、台词是否准确、停顿是否自然、情绪是否温暖笃定、重音是否合适、结尾是否完整，以及是否适合口型和剪辑节奏。

### 9. “声音JSON与VoxCPM2并行”已经写入，但分段策略需前置

现在声音JSON完成后确实可以并行投递Omni原生声音和VoxCPM2，但“片段4/6/8/10秒合法性”和自然时长必须在投递前先验证。以后顺序应是：台词锁定 → 标点节奏稿 → 预估/测试实际时长 → 合法分段 → 声音JSON → 并行生成。

### 10. Skill矩阵登记不等于每个Skill真的执行

批量manifest中有很多Skill行处于`PENDING`或仅登记状态；“106行通过验证”只是结构验证，不代表106个Skill在本条视频中都真实使用。今后每条视频必须区分：真正使用、作为审计、条件不适用、被门禁阻断，并附产物或理由。不能用全局矩阵数量代替实际证据。

### 11. 批量项目还没有真正推进到批量生成

目前只有0001走到独立人物/场景母版和配音技术QC；0002及以后尚未完成各自的对标图生图、人物/场景锁定、分镜和刘泽声音任务。不能把0001的结果当成整批完成。

### 12. 个别旧记录与当前状态并存

Obsidian中保留了历史阻塞记录，这是为了审计，但新窗口必须优先读取最新条目和当前manifest，不能把旧的“Flow连续失败”当作当前状态。需要继续保持“历史记录不可删除，当前状态单独明确”的写法。

## 三、必须修改的新版12阶段流程

### 阶段1：批量输入与跳过规则

读取文件夹、TXT、MP4、参考帧；跳过高考金榜题名；登记每条视频的source_id、原文、时长、参考图和状态。

### 阶段2：现实与视觉总审计

先锁定现实条件和大气视觉方向，再写人物、服装、场景、道具、灯光和文化点睛。没有现实门禁和视觉门禁，不进入生图。

### 阶段3：原文锁定与快节奏拆段

原文保持权威，不擅自润色；按4/6/8/10秒上限拆段；先做时长预估，实际配音超过10秒时重新拆段或重做语速，不得硬截断。

### 阶段4：人物、场景和对标图生图计划

建立人物锁、服装锁、场景锁、道具锁；明确哪张图需要实际图生图；所有Nano Banana Pro请求固定9:16、x4；用户手动执行外部生图。

### 阶段5：用户手动生图提交

本窗口提供：参考图、提示词、模型、比例、x4设置、@me选择方法和提交检查表。用户手动生成4张，返回图片、媒体ID、设置截图和尺寸证据。本窗口不代操作。

### 阶段6：生图后对标与选择

并排比较，评分至少高于对标10%；不合格返工；用户确认人物/场景母版后锁定。

### 阶段7：声音JSON与刘泽配音

标点优化、声音JSON、合法时长检查，然后投递刘泽VoxCPM2；用户或远程Worker生成后返回WAV、时间轴、QC、Hash。本窗口做技术和听感QC。

### 阶段8：分镜表与分镜提示词

从声音时间轴生成S01–S04；每格包含情绪、表情、头部动作、身体动作、手持配置、侧面/过肩关系、运镜、灯光和声音绑定。提示词只写画面，不把台词引号当作画面文字。

### 阶段9：用户手动分镜生图

每个S镜头独立使用Nano Banana Pro、9:16、x4，连续提交，不等待前一个无依赖镜头；用户返回16张候选和证据，本窗口统一筛选。

### 阶段10：分镜审核与视频提示词锁定

用户审核通过分镜后，保持图片提示词和视频提示词的核心设定一致；生成两条不超过10秒的Omni视频提示词，写清每句台词时间码、动作、微表情、运镜和声音JSON。

### 阶段11：用户手动Omni视频生成

用户在Google Flow Omni中手动选择`@me`、模型、时长和9:16并生成；返回视频、设置截图和媒体信息。本窗口不代生成，只做视频QC、连续性和重生成建议。

### 阶段12：剪映装配与最终QC

仅保留一条权威主声音；按实际WAV时长重排字幕、口型、剪辑点和音效；检查快节奏、人物/场景连续性、现实贴合、画面文字、声音、分辨率和平台交付规格。

## 四、以后每个外部生成请求必须带的“手动执行包”

1. 项目和片段ID。
2. 参考图绝对路径及上传顺序。
3. 是否选择`@me`以及选择器操作步骤。
4. 模型：Nano Banana Pro或Omni Flash。
5. 比例：9:16。
6. 图片数量：x4；视频时长：4/6/8/10秒。
7. 可直接复制的一版提示词，禁止多版拼接。
8. 禁止项。
9. 返回证据：设置截图、生成结果、媒体ID、尺寸/时长。
10. 本窗口接收后的比较和QC门禁。

## 五、0001当前准确状态

- 已完成：独立项目、候选03母版、声音JSON、刘泽VoxCPM2技术生成、本机技术QC。
- 待完成：刘泽听感确认；S01–S04各自x4分镜；4K处理；分镜用户审核；Omni两段视频；剪映装配。
- 当前禁止：直接生成视频、把768×1376图标成4K、把11.84秒音频硬塞进10秒、把技术QC当听感QC、把批量项目标成完成。

## 2026-08-07｜DOCX维护规则调整

DOCX不再视为绝对不可修改的文件。允许根据实际执行、模型理解和用户新要求进行局部调整、补充或重排；不得无记录地大幅删减有效细节。每次修改必须登记修改内容、原因、影响范围和版本。当前对外固定展示文档改为三份：口播提示词总规则、流程审计报告、口播视频全要素制作规范；窗口总结继续内部维护，不再作为用户回复附件发送。

## 2026-08-07｜拍摄装置规则补充

新增“人物身体状态”和“拍摄装置/取景方式”分层规则。用户要求人物自己拿手机拍摄时，必须明确：人物本人手持手机、前置摄像头自拍、手机距离、轻微持握漂移、口型清晰；坐着手持手机和边走边手持手机分别写清坐姿或行走状态。不得把人物手机自拍、自拍杆、第三方摄影师手持跟拍和固定机位混写；未得到用户要求时，不自动加入手机自拍。

## 2026-08-07｜拍摄关系与视角来源升级

新增必填字段：拍摄关系、设备持有者、人物视线、设备运动。拍摄关系分为人物自拍、自拍杆自拍、被拍口播、固定机位被拍、第三视角观察和主观POV。它们独立于坐着、站着、走路、奔跑等身体状态。提示词生成前必须先根据用户措辞、场景图和动作要求选择关系；生成后必须检查四个字段是否完整，避免“手持镜头”同时混用自拍、被拍和第三视角。

为确保规则更新真实落地，每次提示词必须经过三项门禁：

1. 字段门禁：提示词中出现拍摄关系、设备持有者、人物视线、设备运动四项。
2. 冲突门禁：检查自拍/被拍/第三视角、身体状态、景别和运镜是否互相冲突。
3. 示例门禁：用户说“我拿手机拍”必须生成自拍关系；用户说“别人拍我”必须生成被拍关系；用户说“从旁边看/第三视角”必须生成第三视角；用户未指定时不得擅自加入手机自拍。
## 2026-08-07｜对标提示词案例库补齐

- 已将用户提供的四段完整对标提示词原文追加到《口播视频全要素制作规范》的独立附录，并保留可编辑同步源：`对标提示词原文与导演式分析附录.md`。
- 附录同时记录共同结构、情绪阶梯、时间轴写法、镜头与动作冲突风险，以及剧情型口播与普通观点口播的调用边界。
- 当前总规则继续作为唯一执行权威；附录只提供案例和方法参考，不恢复已取消的固定三段拍摄模式，也不替代当前的4/6/8/10秒、最多两个主要身体状态和用户动作优先规则。
- DOCX变更已保留为新版本文件，不覆盖原始DOCX；本次修改属于“追加完整案例库与分析”，未删减原DOCX内容。
